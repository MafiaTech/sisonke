namespace Sisonke.Web.Data.Enums;

public enum SubscriptionProvider
{
    // Persisted values: Unknown = 0, Paystack = 1 (existing), Netcash = 2 (reserved for a future adapter).
    Unknown = 0,
    Paystack = 1,
    Netcash = 2
}
